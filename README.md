# Browser Game Prototype

정적 HTML/CSS/JavaScript로 만든 브라우저 게임 프로토타입입니다.

## Local play

`index.html`을 브라우저로 열거나, 로컬 웹 서버에서 실행하세요.

## Controls

- `A` / `D`: move
- `Space`: jump
- Left mouse click: sword attack
- `Tab`: inventory

## Deploy

GitHub Pages는 이 저장소의 기본 브랜치 루트에서 `index.html`을 제공하도록 설정합니다.
